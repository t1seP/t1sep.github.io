# Вимірювання на sepityi.com

Що вже стоїть у коді і що лишається зробити руками. Приблизно 30 хвилин.

---

## Що вже в коді

| Що | Де | Стан |
|---|---|---|
| Контейнер GTM | `index.html`, `thank-you.html`, у `<head>` і після `<body>` | чекає на номер |
| Подія `lead_submit` | `index.html`, спрацьовує при успішній відправці форми | працює |
| Подія `lead_thank_you` | `thank-you.html`, спрацьовує при відкритті сторінки | працює |
| Сторінка подяки | `/thank-you.html` | працює |

GA4 у коді **немає навмисно**. Він підключається всередині GTM. Так усі теги надалі міняються без правок сайту, і це той самий підхід, який ти продаєш клієнтам.

Поки в коді стоїть `GTM-XXXXXXX`, контейнер не завантажується взагалі: там стоїть перевірка. Сайт не робить зайвих запитів і нічого не ламає.

---

## Крок 1. Створити контейнер GTM

1. `tagmanager.google.com` → Створити обліковий запис
2. Назва акаунта: `Тимофій Сепітий`. Країна: Україна
3. Назва контейнера: `sepityi.com`. Платформа: **Веб**
4. Скопіювати номер вигляду `GTM-XXXXXXX`

**Підставити його в двох файлах:**
- `index.html` — рядок `window.GTM_ID = 'GTM-XXXXXXX';` і рядок з `<noscript>`
- `thank-you.html` — так само два місця

Разом чотири заміни. Закомітити й запушити.

---

## Крок 2. Створити ресурс GA4

1. `analytics.google.com` → Адміністратор → Створити ресурс
2. Назва: `sepityi.com`. Часовий пояс: Київ. Валюта: гривня
3. Потік даних → Веб → `https://sepityi.com`
4. Скопіювати ідентифікатор вигляду `G-XXXXXXXXXX`

У код його вставляти **не треба**.

---

## Крок 3. Звʼязати GA4 з GTM

У GTM:

1. Теги → Створити → Тип: **Google Тег**
2. Ідентифікатор тега: твій `G-XXXXXXXXXX`
3. Тригер: **All Pages**
4. Назва тега: `GA4 — базовий`
5. Зберегти

---

## Крок 4. Тригер і тег на заявку

**Тригер:**

1. Тригери → Створити → Тип: **Спеціальна подія**
2. Назва події: `lead_submit` (рівно так, з підкресленням)
3. Назва тригера: `Заявка надіслана`

**Тег:**

1. Теги → Створити → Тип: **Google Аналітика: подія GA4**
2. Тег конфігурації: `GA4 — базовий`
3. Назва події: `generate_lead`
4. Параметри події:
   - `form_name` → `{{DLV - form_name}}`
   - `service` → `{{DLV - service}}`
5. Тригер: `Заявка надіслана`
6. Зберегти

Змінні `{{DLV - form_name}}` і `{{DLV - service}}` створюються так: Змінні → Створити → Змінна рівня даних → назва змінної `form_name` (і окремо `service`).

---

## Крок 5. Перевірити

1. У GTM натиснути **Попередній перегляд**, ввести `https://sepityi.com`
2. Заповнити форму на сайті
3. У вікні перевірки має зʼявитись подія `lead_submit`, а разом з нею спрацювати тег `generate_lead`
4. Далі має відкритись сторінка подяки і спрацювати `lead_thank_you`

Якщо все так — **Надіслати** (опублікувати контейнер). Без публікації нічого не працює на живому сайті.

---

## Крок 6. Позначити конверсію в GA4

1. GA4 → Адміністратор → **Ключові події**
2. Знайти `generate_lead` і ввімкнути перемикач

Подія зʼявиться в списку через кілька годин після першої заявки. До того її можна створити руками: Адміністратор → Події → Створити подію.

---

## Крок 7. Імпорт у Google Ads

Робиться, коли зʼявиться доступ до акаунта Ads і буде що рекламувати.

1. Звʼязати GA4 з Google Ads: GA4 → Адміністратор → Звʼязки з продуктами → Google Ads
2. Google Ads → Цілі → Конверсії → Створити → **Імпорт** → Google Analytics 4 → `generate_lead`
3. Позначити як основну конверсію

Після цього кампанії можна переводити на стратегію за конверсіями. До цього моменту автоматичні стратегії працюють наосліп.

---

## Про подвійний облік

Заявка створює **дві** події: `lead_submit` на формі і `lead_thank_you` на сторінці подяки. Це навмисно, вони страхують одна одну: якщо людина вимкнула JS, спрацює тільки друга; якщо перехід на сторінку подяки не відбувся, лишиться перша.

Конверсією в Ads має бути позначена **тільки одна** з них, інакше кожна заявка порахується двічі. Основною бери `generate_lead` (від `lead_submit`), другу лиши як звичайну подію для перевірки.
